Illustrates the use of the different Thumbnail provider APIs to register thumbnail extraction COM servers for custom file types.  These API provide a mechanism to expose "live" thumbnails as icons for files in Explorer.

Build and register RecipeHandlers.dll and then extract and navigate into the Recipes folder to see live thumbnails for a custom file type that has a ".recipe" extension.