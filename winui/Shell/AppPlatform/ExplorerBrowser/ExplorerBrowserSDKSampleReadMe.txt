This sample illustrates how to consume the shell's implementation of IExplorerBrowser.  It uses shell's IExplorerBrowser implentation to build a browse-for-folder like dialog.

Build and run ExplorerBrowser.exe to see it in action.