This is a sample Event Log querying application. It uses an event query function (EvtQuery) to read events from the event logs.

A QueryLog.vcproj file is created to be used with the Windows SDK. It can be built using Visual Studio 2005 or the "vcbuild.exe" utility.