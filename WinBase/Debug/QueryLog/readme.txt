Event Log Query Sample
======================
     Demonstrates how to query for events from an event log.


Sample Language Implementations
===============================
     This sample is available in the following language implementations:
     C++


To build the sample using the command prompt:
=============================================
     1. Open the Command Prompt window and navigate to the  directory.
     2. Type msbuild [Solution Filename].


To build the sample using Visual Studio:
=======================================
     1. Open Windows Explorer and navigate to the  directory.
     2. Double-click the icon for the .sln (solution) file to open the file in Visual Studio.
     3. In the Build menu, select Build Solution.
     The application will be built in the default \bin or \bin\Debug directory.

Additional Build Steps:
=======================
     1. Use the Visual Studio project file or the makefile.


To run the sample:
=================
     1. Navigate to the directory that contains the new executable, using the command prompt or Windows Explorer.
     2. Type [ExecutableFile] at the command line, or double-click the icon for [SampleExecutable] to launch it from Windows Explorer.


Additional Run Steps:
=====================
     1. Use Visual Studio to run the sample.  There are no command line arguments.

Demonstrates
============
     This sample demonstrates the following:
     Demonstrates the following classes: EvtNext, EvtRender, EvtClose


